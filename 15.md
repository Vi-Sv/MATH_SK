## 🧱 Универсальный архитектурный скелет решения (Свойства НОД / НОК и алгоритм Евклида)
------------------------------
## 1. Входные параметры (Сбор метаданных)

* Спецификация переменных: Пара натуральных чисел (а, b), связанных условиями делимости.
* Связующие операторы:
* НОД(a, b) (Наибольший общий делитель, GCD) — обозначается как d.
   * НОК(a, b) (Наименьшее общее кратное, LCM) — обозначается как m.
* Уравнение структуры: Математическое выражение, связывающее величины a, b, d, m и константы (например: a ⋅ b = K ⋅ d или m + d = C).

------------------------------
## 2. Конвейер вычислений (Алгоритм)

* Шаг 1. Переход к взаимно простым сомножителям (Маневр_Замены)
Любые два числа a и b можно жестко выразить через их наибольший общий делитель d:
1. Задаем подстановку: a = d ⋅ x и b = d ⋅ y.
   2. Критическое ограничение (ОТК): Числа x и y обязаны быть взаимно простыми, то есть их собственный НОД(x, y) = 1. Если это условие нарушено, замена галлюцинирует.
* Шаг 2. Интеграция инварианта произведения (Связь_НОД_НОК)
Используем фундаментальную теорему теории чисел: произведение двух чисел всегда равно произведению их НОД и НОК (a ⋅ b = d ⋅ m). Из этого выражаем НОК через наши новые переменные:
$$m = \frac{a \cdot b}{d} = \frac{(d \cdot x) \cdot (d \cdot y)}{d} = d \cdot x \cdot y$$ 
* Шаг 3. Подстановка и факторизация уравнения (Сжатие_Системы)
Подставляем выраженные значения a, b, m в исходное уравнение задачи. Все слагаемые будут гарантированно содержать делитель d (или его степени).
1. Сокращаем обе части уравнения на d (или d²), упрощая структуру.
   2. Группируем переменные x и y в левой части, перенося чистые константы вправо. Получаем уравнение вида: x ⋅ y = K или f(x, y) = C.
* Шаг 4. Перебор взаимно простых пар (Фильтр_Евклида)
Раскладываем константу в правой части на множители. Составляем список пар (x, y), дающих эту константу.
* Жесткий фильтр: Из списка пар безжалостно удаляем те, у которых НОД(x, y) > 1.
   * Финальный расчет: Для оставшихся валидных пар вычисляем исходные числа: a = d ⋅ x, b = d ⋅ y.

------------------------------
## ⚠️ Архитектурный капкан (ОТК)
Игнорирование взаимной простоты: Самая частая ошибка на отборах — забыть отфильтровать пары (x, y) на предмет их взаимной простоты на Шаге 4. Если x=2 и y=4, то их произведение равно 8, но они не могут быть сомножителями, так как изначальный НОД чисел увеличился бы в 2 раза, полностью разрушая логику Шага 1.
------------------------------
Жду команду: фиксируем скелет №16?

