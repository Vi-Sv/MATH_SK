## 🧱 Универсальный архитектурный скелет решения (Теорема Байеса / Априорный пересчет)
------------------------------
## 1. Входные параметры (Сбор метаданных)

* Априорные гипотезы: Исходные вероятности базовых сценариев до того, как стали известны новые факты. Обозначаются как $P(H_1), P(H_2), \dots, P(H_n)$.
* Контрольная сумма (ОТК): $\sum P(H_i) = 1$.
* Специфика условия: Матрица условных вероятностей наступления факта A при реализации каждой из гипотез: $P(A \vert H_1), P(A \vert H_2), \dots, P(A \vert H_n)$.
* Зафиксированный факт (Событие А): Свидетельство, которое уже произошло на практике (например: «Тест показал ошибку», «Кандидат решил задачу», «Сигнализация сработала»).

------------------------------
## 2. Конвейер вычислений (Алгоритм)

* Шаг 1. Расчет знаменателя (Полная_Вероятность_Факта)
Вычисляем полную вероятность того, что зафиксированный факт A вообще мог произойти в нашей системе. Для этого используем Движок №4 (Формулу полной вероятности):
$$P(A) = P(H_1) \cdot P(A \vert H_1) + P(H_2) \cdot P(A \vert H_2) + \dots + P(H_n) \cdot P(A \vert H_n)$$ 
* Шаг 2. Изоляция целевого вклада (Числитель)
Выбираем одну конкретную гипотезу $H_k$, истинность которой нам нужно проверить после случившегося факта. Находим её чистый вклад в общую структуру:
$$Числитель = P(H_k) \cdot P(A \vert H_k)$$ 
* Шаг 3. Финальный пересчет (Апостериорная_Вероятность)
Делим изолированный вклад целевой гипотезы на общую массу произошедшего факта. Это дает обновленную, реальную вероятность гипотезы с учетом новых данных:
$$P(H_k \vert A) = \frac{P(H_k) \cdot P(A \vert H_k)}{P(A)}$$ 

------------------------------
## ⚠️ Архитектурный капкан (ОТК)
Ловушка интуиции: Апостериорная вероятность $P(H_k \vert A)$ почти никогда не равна условной вероятности факта $P(A \vert H_k)$. Если тест на фрод определяет ИИ-код с точностью 99% ($P(A \vert H_k) = 0.99$), это не значит, что зафиксированный тестом кандидат является фродером с вероятностью 99%. Базовая априорная доля фродеров в общем потоке ($P(H_k)$) может быть ничтожно мала, что полностью изменит баланс долей в Шаге 3.
------------------------------
Жду команду: фиксируем скелет №6?

